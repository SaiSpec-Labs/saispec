from setuptools import setup, find_packages

setup(
    name="saispec",
    version="0.1.0",
    description="The Standard AI Governance & Logic Audit Library.",
    author="SaiSpec Labs",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
)
