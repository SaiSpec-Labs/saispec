from setuptools import setup, find_packages

setup(
    name="saispec",
    version="0.1.0",
    description="The Standard AI Governance & Logic Audit Library.",
    packages=find_packages(), # This finds the 'saispec' folder automatically
    python_requires='>=3.8',
)
